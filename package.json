{
  "type": "module",
  "scripts": {
    "dev": "serve . -p 3000",
    "users:participants": "node --env-file=.env provision-users.mjs participants",
    "users:test": "node --env-file=.env provision-users.mjs test-users",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "supabase": "^2.78.1",
    "vitest": "^2.1.9"
  }
}
